"""
Database migrations for chat persistence.

Each migration is a Python module with `up(conn)` and `down(conn)` async functions.
Migrations are applied in order and tracked in a `_migrations` table.
"""
