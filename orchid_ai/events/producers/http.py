"""HTTP ingestion producer — moved to ``orchid-api``.

The :class:`HTTPIngestionProducer` (FastAPI adapter) lives in
``orchid_api.events.producers.http``.

The ingestion domain types (:class:`SignalSource`,
:class:`SignalSourceRegistry`) are in
:mod:`orchid_ai.events.ingestion`.
"""
