[project]
name = "orchid-ai"
version = "1.8.7"
description = "Orchid — platform-agnostic multi-agent AI framework (LangGraph + RAG)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Francesco Marchesini", email = "gadz82@users.noreply.github.com" },
]
maintainers = [
    { name = "Francesco Marchesini", email = "gadz82@users.noreply.github.com" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Typing :: Typed",
]
keywords = [
    "ai",
    "agents",
    "multi-agent",
    "langgraph",
    "rag",
    "mcp",
    "llm",
    "orchestration",
    "framework",
]
dependencies = [
    "langgraph>=0.4.0",
    "langgraph-checkpoint-sqlite>=3.1.0",
    "langchain-core>=0.3.0",
    "langchain-litellm>=0.5.0",
    "litellm>=1.60.0",
    "httpx>=0.28.0",
    "mcp>=1.9.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "pyyaml>=6.0",
    "aiosqlite>=0.20.0",
    "jmespath>=1.0.0",
    "langchain-text-splitters>=0.0.4",
]

[project.urls]
Homepage = "https://github.com/gadz82/orchid"
Repository = "https://github.com/gadz82/orchid"
Documentation = "https://github.com/gadz82/orchid#readme"
Issues = "https://github.com/gadz82/orchid/issues"
Changelog = "https://github.com/gadz82/orchid/blob/main/CHANGELOG.md"

[project.optional-dependencies]
# ── Heavy / vendor extras ────────────────────────────────────────────
# Document parsing — only needed when the integrator actually parses
# PDF / DOCX / XLSX / image uploads.  ``orchid-api`` and ``orchid-cli``
# pull this in transitively so end users of those packages still get
# upload support out of the box.
documents = [
    "pymupdf>=1.24.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "Pillow>=10.0.0",
]

# Event-driven activation (Pollen + Bloom) — APScheduler is the
# in-process clock for cron / interval triggers.  Only required when
# ``events.enabled: true`` is set in ``agents.yaml``.
events = [
    "apscheduler>=3.10,<4",
]

# Direct Google Generative AI SDK — LiteLLM already routes Gemini, so
# this is opt-in for integrators who want the native Google client
# (e.g. for streaming behaviours not exposed via LiteLLM).
gemini = [
    "google-generativeai>=0.8.3",
]

dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.25.0",
    "pytest-cov>=6.0",
    "ruff>=0.9.0",
    "gitlint>=0.19.0",
    "pre-commit>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["orchid_ai"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=orchid_ai --cov-report=term-missing --cov-fail-under=79"

# ── Semantic Release ─────────────────────────────────────────────

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
branch = "main"
commit_message = "chore(release): v{version} [skip ci]"
build_command = "pip install build && python -m build"
tag_format = "v{version}"

[tool.semantic_release.publish]
upload_to_vcs_release = true
dist_glob_patterns = ["dist/*"]

[tool.semantic_release.changelog]
changelog_file = "CHANGELOG.md"

[tool.semantic_release.commit_parser_options]
allowed_tags = ["feat", "fix", "perf", "refactor", "docs", "style", "test", "build", "ci", "chore"]
minor_tags = ["feat"]
patch_tags = ["fix", "perf"]
